import { Color } from '../../base/common/color';
import { Event } from '../../base/common/event';
import { IDisposable } from '../../base/common/lifecycle';
import { ITokenizationRegistry, ITokenizationSupport, ITokenizationSupportChangedEvent, ILazyTokenizationSupport } from './languages';
export declare class TokenizationRegistry implements ITokenizationRegistry {
    private readonly _tokenizationSupports;
    private readonly _factories;
    private readonly _onDidChange;
    readonly onDidChange: Event<ITokenizationSupportChangedEvent>;
    private _colorMap;
    constructor();
    handleChange(languageIds: string[]): void;
    register(languageId: string, support: ITokenizationSupport): IDisposable;
    get(languageId: string): ITokenizationSupport | null;
    registerFactory(languageId: string, factory: ILazyTokenizationSupport): IDisposable;
    getOrCreate(languageId: string): Promise<ITokenizationSupport | null>;
    isResolved(languageId: string): boolean;
    setColorMap(colorMap: Color[]): void;
    getColorMap(): Color[] | null;
    getDefaultBackground(): Color | null;
}
