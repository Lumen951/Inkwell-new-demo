import { IBoundarySashes } from '../../../../../base/browser/ui/sash/sash';
import { Disposable } from '../../../../../base/common/lifecycle';
import { IObservable } from '../../../../../base/common/observable';
import { DiffEditorOptions } from '../diffEditorOptions';
export declare class DiffEditorSash extends Disposable {
    private readonly _options;
    private readonly _domNode;
    private readonly _dimensions;
    private readonly _sashes;
    private readonly _sashRatio;
    readonly sashLeft: IObservable<number, unknown>;
    private readonly _sash;
    private _startSashPosition;
    constructor(_options: DiffEditorOptions, _domNode: HTMLElement, _dimensions: {
        height: IObservable<number>;
        width: IObservable<number>;
    }, _sashes: IObservable<IBoundarySashes | undefined, void>);
    /** @pure */
    private _computeSashLeft;
}
